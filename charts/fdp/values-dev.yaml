# Development environment values
# Overrides for development deployment

global:
  environment: "dev"

server:
  env:
    clientUrl: "https://gdi-fdp-client-dev.mmcbg.org"
    title: "BG GDI DEV FAIR Data Point"
  
  graphdb:
    url: "http://fdp-graphdb:7200"
    repository: "fdp"

client:
  fdpHost: "fdp-server:80"

gateway:
  enabled: true
  traefik:
    enabled: true
    routes:
      - name: fdp-frontend
        service: fdp-client
        port: 80
        host: "gdi-fdp-client-dev.mmcbg.org"
        pathPrefix: "/"
      - name: fdp-server
        service: fdp-server
        port: 80
        host: "gdi-fdp-server-dev.mmcbg.org"
        pathPrefix: "/"